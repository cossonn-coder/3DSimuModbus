# Testbench Python — simulateur d'I/O Scanner (joue le M580) + tests
# Nico travaille en SSH mobile : dependances minimales, pas d'extra lourd.

pymodbus>=3.6,<4.0   # client Modbus TCP (FC3 lecture retours / FC16 ecriture commandes)
pytest>=8.0          # suite de tests, style HIL (fixtures + assertions explicites)
