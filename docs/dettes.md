# dettes.md — Dettes techniques et simplifications assumées

Règle : chaque entrée a un identifiant, une date, une criticité (bloquant / à
surveiller / cosmétique) et une condition de remboursement. On n'y range PAS les
commentaires pédagogiques (jamais considérés comme dette).

| ID | Date | Criticité | Description | Remboursement |
|---|---|---|---|---|
| D-001 | 2026-07 → **levée 2026-07-14** | ~~À surveiller~~ **Résolue** | FluentModbus **5.3.2** validé par POC (`runtime/poc/`) : serveur FC3/FC16 OK, stable sous scan répété, `lock(server.Lock)` sur méthode **synchrone** suffit (Arch A confirmée). **3 contraintes découvertes** (désormais imposées à `ModbusServer`) : (1) `Span<short>` interdit en méthode `async` → accès buffer synchrone ; (2) le serveur ne sert que l'unit 0 par défaut → `server.AddUnit(unit_id)` + `GetHoldingRegisters(unit)` ; (3) buffer natif little-endian ≠ fil big-endian → **toujours** `Get/SetBigEndian<T>`. Repli mini-serveur maison **non nécessaire**. | Soldée. Détails : `docs/sprints/sprint_01/NOTES.md`. |
| D-002 | 2026-07 | Cosmétique | Collision latérale non simulée : si un vérin sort pendant qu'une palette franchit exactement le point d'arrêt, la palette n'est bloquée que si son bord avant n'a pas franchi le point. | V2 si besoin réel constaté par l'automaticien. |
| D-003 | 2026-07 | Cosmétique | Convoyeur sans rampe (arrêt/démarrage instantanés des palettes). `feedback_delay_ms` ne modélise que le contacteur. | V2 si les temporisations PLC l'exigent. |
| D-004 | 2026-07 | À surveiller | FC23 (ligne RD+WR combinée) non supporté/testé en V1 — choix assumé : deux lignes de scan. | Si l'automaticien veut une ligne combinée : valider FC23 de la lib ou l'implémenter. |
| D-005 | 2026-07 | À surveiller | Pas de validation de schéma JSON formalisée (JSON Schema) : le chargeur Godot doit être défensif à la main en V1. | Introduire un JSON Schema quand le pipeline Python (Phases 2-3) produira le fichier. |
| D-007 | 2026-07-14 | À surveiller | Le testbench avait été écrit contre l'API pymodbus `slave=` (versions <3.7), **supprimée en 3.14**. Corrigé en `device_id=` (`io_scanner_sim.py`, `test_modbus_chain.py`) ; `requirements.txt` remonté à `>=3.7`. Le plancher exact où `device_id` apparaît n'a pas été vérifié finement (mis à 3.7 par prudence). | Confirmer le plancher réel si un env impose une vieille pymodbus ; sinon épingler une version testée. |
| D-009 | 2026-07-15 | Cosmétique | `render.kind` (ex. `"ring"`) **non consommé** par le builder 3D : le convoyeur est câblé « anneau CSG » en dur (`CarrouselScene.BuildConveyor`), légitime en V1 (un seul convoyeur, un seul type de rendu). Idem : les vérins/palettes/fenêtres ont leur forme codée par type de composant, pas piloté par un champ `kind`. | Généraliser (dispatch par `kind`) le jour où un pivot déclare un rendu différent. |
| D-010 | 2026-07-15 → **levée 2026-07-15** | ~~À surveiller~~ **Résolue** | Smoke-test headless de la scène 3D non exécuté (Godot absent au sprint 1). **Soldée au sprint 2** : `smoke_anim.ps1` vert (recensement `ring=1 cylinders=2 pallets=3 sensors=2` + tige montée + palette avancée + heartbeat incrémenté) **et** validation visuelle par Nico (rotation CCW, postes 90°/270°, extension/rappel de tige, accumulation). | Soldée. Détails : `docs/sprints/sprint_02/NOTES.md` §6. |
| D-011 | 2026-07-15 | Cosmétique | **Duplication du câblage runtime** `StepSim` (`CarrouselScene`) ↔ boucle de `SimHost` : les deux enchaînent `PullCommands → sim.Tick → PushReturns` sans factorisation. Choix **assumé** (2 sites ; le core ne peut voir ni le serveur ni Godot, aucune abstraction ne s'impose). | Factoriser (petit hôte partagé) **uniquement** si un 3ᵉ site apparaît. |
| D-012 | 2026-07-15 | À surveiller | **Throttling d'une fenêtre non focalisée** : l'OS peut réduire la cadence d'une fenêtre Godot en arrière-plan → le heartbeat pourrait ralentir même avec `low_processor_mode=false`. Non entièrement maîtrisable côté Godot. Non observé comme bloquant à ce jour. | Si constaté gênant en démo : replier la sim sur un **thread dédié** (façon SimHost intégré) + verrou sur l'état visuel lu par `ApplyToScene`. Écarté ce sprint (casse « même thread », complexité non justifiée). |
| D-013 | 2026-07-15 → **levée 2026-07-16** | ~~À surveiller~~ **Résolue** | **Échec silencieux de `server.Start()`** : si le port 502 est déjà pris (ex. un `SimHost` reliquat), la scène se construisait mais son serveur ne démarrait pas — **aucun message à l'écran**, la maquette semblait figée. **Soldée au sprint 3** : bind occupé désormais **bruyant** (S3.1 : pré-vol `TcpListener` + try/catch → `ModbusServerException`, échec confirmé **synchrone** en FluentModbus 5.3.2, test qui reproduit D-013 écrit **avant** le correctif) **et visible** (S3.2 : bandeau rouge non masquable + `GD.PrintErr`, scène non plantée). | Soldée. Détails : `docs/sprints/sprint_03/NOTES.md` §2. |
| D-014 | 2026-07-15 | Cosmétique | **`run/low_processor_mode=false` non versionnable durablement** : Godot **élague les réglages égaux au défaut** en réécrivant `project.godot` à chaque session éditeur ; `false` étant le défaut, la ligne (garde-fou heartbeat, S2.1) **disparaît** dès que Nico ouvre l'éditeur → diff parasite récurrent. **Sans impact fonctionnel** (le défaut `false` donne le comportement voulu : boucle à chaque frame). Le « verrou » explicite contre un futur passage à `true` n'est donc pas persistant. | Accepter (la protection réelle = le défaut) ou, si un jour le mode est vraiment forcé ailleurs, re-committer la ligne. Ne pas s'acharner à la garder à chaque save. |
| D-006 | 2026-07-14 | À surveiller | Testabilité de la logique C# (loader pivot + datastore) **hors Godot** : un projet `Godot.NET.Sdk` se référence mal depuis un projet xUnit classique. Décision **(b)** actée : extraire `PivotModel` + `ModbusDataStore` dans un **class library .NET séparé** (`CarrouselCore`), référencé par l'assembly Godot **et** par un projet xUnit — symétrie avec pytest côté Python. Justification : CLAUDE.md acte déjà « logique de simulation et datastore testables sans lancer Godot » ; l'option (a) (tests différés, smoke-test headless seul) contournerait cet engagement dès le 1ᵉʳ sprint qui peut le tenir. Coût réel = un `.csproj` de plus (scaffoldé, pas tapé à la main), pas de complexité de code. | Résiduel à surveiller : la structure multi-projets doit continuer à builder proprement sous Godot (`ProjectReference` core → assembly Godot). Réévaluer si le build Godot s'en trouve gêné. |
| D-015 | 2026-07-16 | À surveiller | **Navigation 3D + vitesse de simulation réglable absentes.** La caméra est figée (`AddPresentation`, `LookAt` fixe) : pas de zoom ni d'orbite libre. La cadence de sim est le pas fixe 100 ms (heartbeat 10 Hz) : pas de réglage de vitesse pour la démo. Demandé par Nico (2026-07-16) comme ergonomie de démonstration. **Scindée à la conception du sprint 4 (2026-07-16)** : la **partie navigation** (caméra orbitale libre) est traitée au **sprint 4** (« ergonomie d'utilisation », S4.1) ; la **partie vitesse réglable** reste **reportée** (non nécessaire à la navigation, et régler la vitesse ≠ dérégler le heartbeat que le M580 surveille). | Navigation : **soldée au sprint 4** (à cocher à la clôture). Vitesse : sprint ultérieur — **découpler le facteur *visuel* du pas de tick** (garder le heartbeat à 10 Hz), à cadrer en conception. |
| D-016 | 2026-07-16 | À surveiller | **Pas d'édition in-app de la machine.** Impossible d'ajouter un élément (vérin, capteur…) et de le brancher à l'automate (mapping `{zone, word, bit}`) depuis le HUD : la machine est entièrement définie par le pivot écrit à la main. Demandé par Nico (2026-07-16). Rejoint la **Phase 5 (IHM automaticien)** du backlog. | Gros morceau à concevoir en propre : éditeur de composants + mapping **qui produit du pivot depuis l'UI** (le contrat central écrit par l'interface). Implique validation de schéma (cf. D-005) et probablement le forçage/écriture (reporté du Sprint 3). |
| D-018 | 2026-07-16 | Cosmétique | **Champs de nœuds 3D vestigiaux dans `CarrouselScene`** : `_ringNode`, `_cyl1Node`, `_cyl2Node`, `_sensor1Node`, `_sensor2Node` (ancres héritées des `Label3D` S3.3, retirés en S4.2) sont désormais **assignés mais jamais relus**. L'amorce S4.3 prévoyait de brancher le picking dessus ; le sous-agent a préféré poser l'`Area3D` **dans les builders sur le nœud local** de chaque composant (`AttachHoverArea`) — approche **plus générique** (une zone par composant du pivot, pas de `_cyl1/2` codés en dur, cf. §0bis), qui rend ces champs orphelins. Conservés (retrait = refactor hors périmètre S4.3, cf. CLAUDE.md §4). | Supprimer les 5 champs + leurs assignations lors d'un prochain passage sur `CarrouselScene` (aucune logique n'en dépend). |
| D-017 | 2026-07-16 | À surveiller | **Pas de simulation de cas non nominaux (éléments défaillants).** La cinématique est purement nominale : aucun moyen d'injecter une panne (capteur bloqué à 0/1, vérin qui ne sort pas ou reste coincé mi-course, convoyeur qui patine, retour `ret` incohérent avec la commande, coupure de retour). Demandé par Nico (2026-07-16) pour éprouver la robustesse du programme automate face aux défauts. | Sprint dédié « injection de défauts » : par élément, un mode de défaillance paramétrable (via HUD, cf. D-016, ou via le pivot). À concevoir : où vit l'état « défaillant » (sim pure, testable) et comment le piloter sans casser l'invariant « l'app sert, ne décide pas » (un défaut n'est pas une décision métier, c'est un état physique forcé). |
